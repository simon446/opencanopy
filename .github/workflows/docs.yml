name: Docs build & publish

# Builds a browsable documentation site from docs/ and publishes it to GitHub Pages on main
# (spec §14.3 documentation requirements). On PRs it builds only, as a gate that the docs render.
# If the Documentation track later commits an mkdocs.yml, that config is used as-is; otherwise a
# minimal config is generated on the fly so the workflow works before site config is owned.

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "README.md"
      - "mkdocs.yml"
      - ".github/workflows/docs.yml"
      - "mechanical/stl/assembly/**"
      - "mechanical/cad/source/render.py"
  pull_request:
    paths:
      - "docs/**"
      - "README.md"
      - "mkdocs.yml"
      - ".github/workflows/docs.yml"
      - "mechanical/stl/assembly/**"
      - "mechanical/cad/source/render.py"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build docs site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install MkDocs
        run: pip install --quiet mkdocs mkdocs-material

      - name: Render full-assembly views
        # Best-effort refresh: the VTK-rendered PNGs are committed and validated, so a
        # headless-rendering hiccup never blocks the Pages deploy — the committed images
        # are published as-is in that case.
        continue-on-error: true
        # Reads the committed placed meshes in mechanical/stl/assembly/ and renders
        # them with VTK — a real z-buffered renderer (correct occlusion, uniform faces,
        # clean feature edges). No OpenCascade/build123d needed. vtk-osmesa is the
        # headless software-rendering wheel (no X server / GPU), so `import vtk` works
        # offscreen in CI. Refreshes docs/assets/renders/*.png to match the geometry.
        run: |
          pip install --quiet trimesh vtk-osmesa
          python mechanical/cad/source/render.py

      - name: Generate fallback mkdocs.yml if none committed
        shell: bash
        run: |
          set -euo pipefail
          if [ -f mkdocs.yml ]; then
            echo "Using committed mkdocs.yml"
          else
            echo "No mkdocs.yml — generating a minimal fallback config."
            cat > mkdocs.yml <<'YAML'
          site_name: OpenCanopy Documentation
          site_description: Tabletop automated pepper grower — open hardware
          theme:
            name: material
          docs_dir: docs
          use_directory_urls: false
          YAML
          fi

      - name: Build
        run: mkdocs build --site-dir _site

      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
